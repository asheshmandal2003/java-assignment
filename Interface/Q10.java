package Interface;

public interface Q10 {

    /*
     * We can't use private as a field variable in an interface.
     */

    // private int a = 10;

    public int b = 20;

    /*
     * We can't use protected as a field variable in an interface.
     */

    // protected int c = 30;
}
